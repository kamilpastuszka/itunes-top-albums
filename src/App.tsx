import React from 'react';
import Header from './Header';

const App: React.FC = () => {
    return (
        <div className="jumbotron">
            <Header />
        </div>
    );
};

export default App;
